[package]
name = "geneaquilt-core"
version = "0.1.0"
description = "GEDCOM parsing and genealogy graph domain model for GeneaQuilt"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true

[lib]
path = "src/lib.rs"

[dependencies]
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
serde_json = "1"
