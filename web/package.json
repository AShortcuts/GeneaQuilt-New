{
  "name": "geneaquilt-web",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build:wasm": "PATH=\"$HOME/Library/Caches/.wasm-pack/wasm-bindgen-cargo-install-0.2.118:$PATH\" wasm-pack build ../crates/geneaquilt-wasm --target web --out-dir ../../web/pkg --release --mode no-install",
    "dev": "npm run build:wasm && vite",
    "build": "npm run build:wasm && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^7.1.0"
  }
}
